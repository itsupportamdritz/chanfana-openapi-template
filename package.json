{
  "name": "chanfana-openapi-template",
  "description": "Complete backend API template using Hono + Chanfana.",
  "scripts": {
    "cf-typegen": "wrangler types",
    "deploy": "wrangler deploy",
    "schema": "npx chanfana",
    "test": "wrangler deploy --dry-run && npx vitest run --config tests/vitest.config.mts"
  },
  "dependencies": {
    "chanfana": "2.8.1",
    "hono": "4.8.2",
    "zod": "3.25.67"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.8.44",
    "@types/node": "24.0.4",
    "typescript": "5.8.3",
    "wrangler": "4.21.x"
  }
}
